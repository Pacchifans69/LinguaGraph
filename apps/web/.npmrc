# LinguaGraph resolves dependencies from the official npm registry so the
# committed package-lock.json stays canonical and portable.
# (See M0.1 human review: remove environment-specific mirror URLs from the lockfile.)
registry=https://registry.npmjs.org/
