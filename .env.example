# LinguaGraph — root environment example (non-secret development values only).
# Copy to `.env` for Docker Compose; never commit `.env`.

# PostgreSQL service (used by compose.yml)
POSTGRES_USER=linguagraph
POSTGRES_PASSWORD=linguagraph
POSTGRES_DB=linguagraph
POSTGRES_PORT=5432
